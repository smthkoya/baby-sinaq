    1. Which mechanism provides micro-segmentation for pod-to-pod traffic inside an AKS cluster?
    • Application security groups on the node NICs
    √ Kubernetes network policies
    • Network security groups on the node subnet
    • Pod Security Standards
    • Azure Firewall DNAT rules

    2. Why must the Lab 4.2 route table NOT be associated with the jump-host subnet?
    • Route tables can only be associated with one subnet at a time
    • The jump subnet uses IPv6, which route tables do not support
    • Azure Firewall rejects traffic originating from jump hosts
    √ Routing the jump subnet's traffic through the firewall would break the inbound RDP session's return path
    • The jump subnet already has a higher-priority system route

    3. Which statement about Key Vault permission models is correct for new deployments?
    • Azure RBAC and access policies can be mixed per user on the same vault
    √ Azure RBAC is the recommended model, using data-plane roles like Key Vault Secrets User
    • Vault access policies are recommended because they integrate with PIM
    • Neither model applies to secrets, only to keys
    • Access policies are required for managed identities to read secrets

    4. In Lab 4.2, what makes workload-subnet traffic actually pass through the Azure Firewall?
    • Enabling forced tunneling on the VNet
    • Peering the workload subnet to AzureFirewallSubnet
    • Associating the firewall with the workload subnet in the firewall settings
    • A DNAT rule translating the firewall public IP to the workload VMs
    √ A user-defined route for 0.0.0.0/0 with next hop type Virtual appliance and the firewall's private IP

    5. What changes when a storage account moves from platform-managed keys to a customer-managed key in Key Vault?
    • Encryption moves from the service layer to the client application
    • The encryption algorithm upgrades from AES-128 to AES-256
    • The storage account no longer needs HTTPS
    √ The customer controls key rotation, auditing, and can revoke access, making data unreadable
    • Data is re-encrypted twice for every write operation

    6. In what order does Azure Firewall process its rule types?
    • Rules are processed strictly by collection priority regardless of type
    √ DNAT, network, application
    • DNAT, application, network
    • Application, network, DNAT
    • Network, DNAT, application

    7. In a DevSecOps pipeline, which gate detects vulnerable open-source packages pulled in by the application?
    • DAST
    • Secret scanning
    • IaC scanning
    √ Software composition analysis (SCA)
    • SAST

    8. Azure Web Application Firewall managed rules are primarily built on which foundation?
    • Suricata signature packs
    • NIST CSF categories
    √ The OWASP Core Rule Set
    • CIS Benchmarks
    • MITRE ATT&CK detections

    9. Which Azure capability protects data IN USE, not just at rest or in transit?
    • Azure Disk Encryption with BitLocker
    • Storage Service Encryption with platform keys
    • TLS 1.2 enforcement on storage accounts
    √ Confidential VMs with hardware-based trusted execution environments
    • Double encryption of storage infrastructure

    10. Which two properties must be added to an .rdp file for Entra-authenticated RDP to a Windows VM?
    • authentication level:i:0 and gatewayusagemethod:i:2
    • enablecredsspsupport:i:1 and authentication level:i:0
    • aadauth:i:1 and networklevelauth:i:0
    √ enablecredsspsupport:i:0 and authentication level:i:2
    • use redirection server name:i:1 and prompt for credentials:i:1

    11. An NSG evaluates its rules in which way?
    • Deny rules always evaluate before allow rules regardless of priority
    √ By priority number, lowest first, and processing stops at the first match
    • Alphabetically by rule name
    • All matching rules are combined and the most restrictive wins
    • By priority number, highest first, and processing stops at the first match

    12. Which firewall rule type can allow outbound traffic to www.bing.com while denying all other websites?
    √ An application rule matching the target FQDN
    • A network rule with destination port 443
    • An NSG outbound rule with a service tag
    • A DNAT rule for the workload subnet
    • A route table entry for bing.com's IP range

    13. Which default NSG inbound rule causes internet RDP attempts to a VM to fail when no custom allow rule matches?
    • DenyAllOutBound (65500)
    • AllowVnetInBound (65000)
    √ DenyAllInBound (65500)
    • AllowAzureLoadBalancerInBound (65001)
    • BlockInternetInBound (65400)

    14. What is the main operational benefit of using Application Security Groups in NSG rules?
    • They encrypt traffic between the grouped virtual machines
    • They replace the need for any NSG on the subnet
    • They increase network throughput between members
    • They extend NSG filtering across multiple regions
    √ Rules reference workload roles instead of IP addresses, so adding a VM needs no rule changes

    15. Which service absorbs volumetric layer-3/4 floods but does NOT stop SQL injection attacks?
    • Azure Web Application Firewall
    • Azure Front Door rules engine
    • Azure Key Vault firewall
    • Network Security Groups
    √ Azure DDoS Network Protection
