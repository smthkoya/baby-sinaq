    1. A backup restore process requires approval from two managers before data can be recovered. Which CIA trade-off does this design illustrate?
    • Confidentiality is reduced to improve integrity
    • Integrity is reduced to improve availability
    • Integrity is exchanged for scalability
    √ Availability is reduced to strengthen confidentiality and integrity
    • Confidentiality and availability both increase without cost

    2. A ministry keeps citizen records in a national government data center with cloud-style self-service, while using Microsoft 365 for staff email. Which deployment model describes the overall environment?
    • Private cloud
    • Public cloud
    • Community cloud
    • Multi-tenant cloud
    √ Hybrid cloud

    3. Which list contains only pillars from the six foundational pillars of Zero Trust?
    • Subscriptions, tenants, portals
    • Compliance, audit, governance
    • Firewalls, proxies, gateways
    √ Identities, devices, applications
    • Backups, snapshots, archives

    4. Which cross-cloud mapping is most accurate for governance hierarchies?
    √ Azure management group maps to AWS Organizations OU and GCP folder
    • AWS account maps to an Azure resource tag
    • Azure tenant maps to a single AWS EC2 instance
    • GCP project maps to an Entra directory role
    • Azure subscription maps to an AWS IAM user

    5. Place the defense correctly on the attack timeline: which control primarily blocks the initial access (foothold) phase?
    • Data loss prevention policies
    • Network micro-segmentation between subnets
    • Immutable backup storage
    √ Multifactor authentication enforced by Conditional Access
    • Just-in-time privileged role activation

    6. Which layer sequence correctly represents the defense-in-depth model taught for cloud environments?
    • Data, application, licensing, billing, tenant, portal, physical
    √ Physical, identity and access, perimeter, network, compute, application, data
    • Identity, subscription, resource group, resource, tag, lock, policy
    • Perimeter, firewall, antivirus, patching, backup, audit, insurance
    • Reconnaissance, foothold, escalation, movement, exfiltration

    7. In July 2026, where does an administrator assign an Office 365 E3 license to a group?
    • Azure portal, under Subscriptions
    • Microsoft Entra admin center, under Billing
    • Microsoft Defender portal, under Settings
    • Microsoft Purview portal, under Data governance
    √ Microsoft 365 admin center, under Billing then Licenses

    8. Which statement about a Microsoft Entra tenant and an Azure subscription is correct?
    √ A tenant can contain many subscriptions, and each subscription trusts exactly one tenant
    • Licenses are purchased per subscription, not per tenant
    • A subscription can trust many tenants at the same time
    • A tenant and a subscription are two names for the same object
    • Deleting a subscription also deletes the tenant

    9. Why are stored passwords salted before hashing?
    • To compress the password database
    • To synchronize passwords between cloud providers
    • To make the password longer and easier to remember
    • To allow administrators to decrypt passwords when needed
    √ To defeat precomputed dictionary and rainbow-table attacks

    10. Which attack tries a small set of common passwords against many accounts to stay under lockout thresholds?
    • Dictionary attack against a single account
    • SQL injection
    • Token replay
    √ Password spray
    • Phishing

    11. A dynamic security group was created with the rule user.objectid -ne null, and saving fails with an invalid-property error. What is the most likely cause?
    • The group owner lacks an Office 365 license
    • Dynamic groups require a Microsoft 365 group type
    √ The rule property is case-sensitive and must be written user.objectId
    • Dynamic groups cannot use the -ne operator
    • The tenant has too many users for dynamic evaluation

    12. In a TLS connection to entra.microsoft.com, how are asymmetric and symmetric cryptography used together?
    • Symmetric establishes the handshake; asymmetric encrypts the session data
    • Asymmetric encrypts all traffic; symmetric is used only for signatures
    • Both encrypt every packet twice for extra security
    √ Asymmetric secures the handshake and key exchange; a symmetric session key encrypts the traffic
    • TLS uses hashing only and no encryption

    13. A procurement document requires cloud-specific extensions of ISO/IEC 27001 covering protection of personal data in public clouds. Which standard should be cited?
    √ ISO/IEC 27018
    • ISO 9001
    • ISO/IEC 20000
    • ISO/IEC 27017
    • ISO 31000

    14. Which set lists the five essential characteristics of cloud computing as defined by NIST SP 800-145?
    √ On-demand self-service, broad network access, resource pooling, rapid elasticity, measured service
    • Virtualization, containers, APIs, automation, billing
    • Public, private, hybrid, community, multi-cloud
    • IaaS, PaaS, SaaS, FaaS, DBaaS
    • Confidentiality, integrity, availability, authentication, authorization

    15. Which option states the three guiding principles of Zero Trust?
    • Authenticate, authorize, audit
    √ Verify explicitly, use least-privilege access, assume breach
    • Encrypt everything, log everything, back up everything
    • Prevent, detect, respond
    • Isolate, segment, monitor
