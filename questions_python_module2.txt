    1. Which token does an application present to Microsoft Graph when calling the API on a user's behalf?
    • The primary refresh token
    • The SAML assertion
    • The ID token
    √ The access token
    • The refresh token

    2. A user signs in successfully but receives "You do not have access" when opening a storage account. Which statement is correct?
    • Authentication failed and must be investigated first
    • The storage account firewall rejected the token
    √ Authentication succeeded but authorization failed
    • Authorization succeeded but authentication failed
    • Both authentication and authorization failed

    3. Where do you configure which authentication methods users may register and use in a modern tenant?
    • Users > Per-user MFA
    • Conditional Access > Named locations
    √ Protection > Authentication methods > Policies in the Entra admin center
    • Azure portal > Subscriptions > Access control
    • Password reset > Customization

    4. An attacker tries a few common passwords against thousands of accounts to avoid lockouts. What is this attack called?
    • MFA fatigue
    • Pass-the-hash
    • Golden ticket
    • Credential stuffing
    √ Password spray

    5. You must require MFA for one pilot user accessing Office 365 only. What is the correct modern implementation?
    • Enable SSPR for a group containing the user
    • Assign the user the Authentication Administrator role
    • Set the user's per-user MFA state to Enforced
    • Enable security defaults for the tenant
    √ Create a Conditional Access policy targeting that user and Office 365 with the Require MFA grant control

    6. Which built-in Azure role can manage all resources but cannot grant access to others?
    • Owner
    • Global Administrator
    • User Access Administrator
    • Reader
    √ Contributor

    7. What does OpenID Connect add on top of OAuth 2.0?
    √ An authentication layer with the ID token
    • Network location conditions
    • Role-based authorization for Azure resources
    • Password hash synchronization
    • XML-based assertions

    8. Enabling Microsoft Authenticator in the Authentication methods policy has what effect by itself?
    • SSPR is enabled for all users
    • Passwords are disabled tenant-wide
    √ The method becomes available, but nothing enforces MFA yet
    • All users must use MFA at next sign-in
    • Legacy per-user MFA is automatically migrated

    9. Which three components combine to form an Azure RBAC role assignment?
    • Tenant, license, and resource group
    √ Security principal, role definition, and scope
    • User, password, and subscription
    • Group, directory role, and management group
    • Role definition, Conditional Access policy, and scope

    10. Which statement about password hash synchronization (PHS) is true?
    • Passwords are validated by on-premises agents at every cloud sign-in
    √ A salted, iterated hash of the password hash is synced, and cloud sign-in works even if on-premises AD is down
    • PHS requires deploying AD FS federation servers
    • PHS only works with verified .local domains
    • Clear-text passwords are uploaded to Microsoft Entra ID over TLS

    11. Which AWS concept is the closest equivalent of an Azure RBAC role definition?
    • A VPC endpoint policy only
    √ An IAM policy document
    • A Security Group
    • A NACL
    • An S3 bucket ACL

    12. Before enabling a new Conditional Access policy broadly, which two tools give you evidence of its impact?
    • Check access and Resource health
    √ Report-only mode and the What If tool
    • Smart lockout and app passwords
    • Azure Advisor and Cost analysis
    • Synchronization Service Manager and dcdiag

    13. An on-premises forest uses the non-routable suffix contoso.local. What happens to user UPNs when they sync to Entra ID without further configuration?
    √ Entra ID substitutes the tenant's onmicrosoft.com domain as the UPN suffix
    • Synchronization fails until AD FS is installed
    • Entra Connect automatically registers contoso.local as a verified domain
    • The users are created as B2B guests
    • The users keep contoso.local and cannot sign in at all

    14. Which Microsoft Authenticator feature specifically defeats MFA-fatigue (push bombing) attacks?
    • Location on the lock screen
    • Cloud backup
    √ Number matching
    • App passwords
    • QR-code enrollment

    15. A Global Administrator opens the Azure portal and sees no virtual machines in the subscription. Why?
    • The VMs are hidden until MFA is completed
    • Global Administrators are blocked from subscriptions by default policy
    √ Entra directory roles do not grant Azure RBAC permissions on resources
    • Only break-glass accounts can view resources
    • The subscription must first be registered in the Entra admin center
