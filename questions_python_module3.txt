    1. How does ZTNA differ from a traditional VPN?
    • ZTNA works only for Microsoft applications
    • ZTNA requires no authentication after initial setup
    √ ZTNA brokers access to individual applications instead of granting routable access to a network segment
    • ZTNA replaces the identity provider entirely
    • ZTNA encrypts traffic while VPNs do not

    2. An administrator configures a domain deny list in collaboration restrictions. What happens to guests from that domain who already redeemed their invitations?
    • Their sessions are revoked within one hour
    • They are converted to member users
    √ They keep their access because the list only gates new redemptions
    • They are deleted immediately
    • They are moved to a quarantine group

    3. In entitlement management, what is an access package?
    • A group of Conditional Access policies applied together
    • A template for bulk-inviting guests
    • A billing bundle of Entra licenses
    • A compressed export of audit logs
    √ A requestable bundle of resource roles from a catalog, governed by request, approval, and lifecycle policies

    4. Which guest user access restriction level prevents guests from seeing any users, groups, or memberships other than their own profile?
    • Guest users are blocked from all directory read operations
    • Guest users can read only hidden group memberships
    • Guest users have the same access as members
    • Guest users have limited access to properties and memberships of directory objects
    √ Guest user access is restricted to properties and memberships of their own directory objects

    5. Which Microsoft Graph permission scope is the least-privilege choice for creating B2B invitations with Connect-MgGraph?
    • Group.ReadWrite.All
    √ User.Invite.All
    • Application.ReadWrite.All
    • Directory.ReadWrite.All
    • User.Read

    6. Why must break-glass emergency accounts be excluded from PIM and Conditional Access?
    • Excluding them reduces licensing costs
    √ They must remain usable when PIM, CA, or the IdP itself is misconfigured or unavailable
    • Their passwords cannot satisfy MFA requirements
    • They lack the licenses required for PIM
    • Microsoft blocks policy assignment on them automatically

    7. During Google federation setup, which set of authorized redirect URIs must be registered in the Google OAuth client?
    • The tenant's custom domain and its MX record host
    • https://portal.azure.com and https://myapps.microsoft.com
    √ https://login.microsoftonline.com plus the two /te/.../oauth2/authresp URIs built from the tenant ID and tenant name
    • Any URI, because Google does not validate redirects
    • Only https://entra.microsoft.com

    8. Under which Entra admin center section is Privileged Identity Management found?
    • Global Secure Access
    • Entra ID > Monitoring & health
    • Learn & support
    √ ID Governance
    • Protection

    9. After Google federation is configured, how does the redemption experience change for a gmail.com invitee?
    • They receive an email one-time passcode as before
    • They must create a new Microsoft account
    √ They authenticate at accounts.google.com and no passcode or verification email is needed
    • Their invitation is auto-redeemed without any sign-in
    • They must register a FIDO2 key first

    10. In Microsoft Entra B2B collaboration, where do a guest user's credentials live and get managed?
    • In a shared Microsoft-operated credential vault
    √ At the guest's home identity provider
    • In the guest object's extension attributes
    • In the resource tenant that sent the invitation
    • In Azure Key Vault of the resource tenant

    11. Which set of requirements can a PIM role's activation policy impose on a user activating an eligible Azure resource role?
    √ MFA, justification, ticket information, and approval
    • Manager consent, HR record update, and background check
    • Password reset, device enrollment, and license assignment
    • Group membership, dynamic rule match, and access package request
    • Break-glass exclusion, tenant restriction, and OTP

    12. In PIM, what is the key difference between an eligible and an active role assignment?
    • Eligible assignments bypass Conditional Access
    √ Eligible assignments grant nothing until the user activates the role
    • Active assignments expire after one hour automatically
    • Active assignments require approval at every use
    • Eligible assignments grant a reduced permission set

    13. In the Entra admin center, which navigation path opens the bulk guest invitation operation?
    • ID Governance > Entitlement management > Bulk invite
    • Protection > Identity Protection > Bulk actions
    • Entra ID > Groups > Bulk operations > Import members
    • Entra ID > External Identities > Cross-tenant access settings
    √ Entra ID > Users > All users > Bulk operations > Bulk invite

    14. Which redemption method acts as the universal fallback when an invitee has no Entra, Microsoft, Google, or SAML/WS-Fed identity?
    • FIDO2 security key
    √ Email one-time passcode
    • Certificate-based authentication
    • SMS verification code
    • Temporary Access Pass

    15. Which PowerShell approach is currently supported for scripting guest invitations?
    • New-AzureADMSInvitation from the AzureAD module
    √ New-MgInvitation from Microsoft Graph PowerShell
    • New-AzRoleAssignment from the Az module
    • New-MsolUser from the MSOnline module
    • Add-MailboxPermission from Exchange Online PowerShell
