    1. Azure reserves how many IP addresses in every subnet?
    √ Five
    • Two
    • Four
    • Three
    • Eight

    2. A /24 subnet contains 256 total addresses. How many are usable in Azure after platform reservations?
    • 250
    • 252
    • 254
    √ 251
    • 256

    3. Two VNets have overlapping address spaces. What is the main consequence?
    • NSGs stop being stateful
    • Azure Firewall disables DNAT
    • They automatically merge routes
    √ They cannot be peered directly
    • Bastion is required

    4. Spoke A is peered to a hub, and spoke B is also peered to the same hub. Why can A not automatically reach B?
    • Azure reserves the hub route
    • Spokes require public IPs
    • NSGs always deny peering
    √ VNet peering is non-transitive
    • All peering is internet-routed

    5. Which Azure feature overrides system routes for selected prefixes and can point traffic to a firewall or NVA?
    √ User-defined route
    • Access package
    • Application Security Group
    • Managed identity
    • Sensitivity label

    6. A workload subnet must send all internet-bound traffic through an Azure Firewall at 10.0.1.4. Which route is required?
    √ 0.0.0.0/0 with next hop Virtual appliance 10.0.1.4
    • 0.0.0.0/0 with next hop VNet peering
    • An ASG with 10.0.1.4
    • 10.0.1.4/32 with next hop Internet
    • A DNAT rule only

    7. Why is an Azure Firewall ineffective for egress inspection if no UDR points workload traffic to it?
    • NSGs disable the firewall
    • Azure Firewall only supports inbound traffic
    • The firewall cannot process TCP
    • The firewall loses its public IP
    √ Traffic follows other routes and bypasses the firewall

    8. How does an NSG evaluate custom rules?
    • Highest priority number first
    • All rules are combined and the most restrictive wins
    • Deny rules always run first regardless of priority
    • Rules are processed alphabetically
    √ Lowest priority number first, stopping at the first match

    9. Which default NSG inbound rule blocks internet traffic when no earlier allow rule matches?
    • AllowAzureLoadBalancerInBound (65001)
    • DenyAllOutBound (65500)
    • BlockInternetInBound (65400)
    √ DenyAllInBound (65500)
    • AllowVnetInBound (65000)

    10. What does stateful NSG behavior mean?
    • NSGs dynamically change IP addresses
    • Every packet must match both inbound and outbound rules independently
    • NSGs remember user passwords
    √ Return traffic for an allowed connection is automatically permitted without a separate reverse rule
    • NSGs inspect HTTP content

    11. What is the main advantage of Application Security Groups?
    • They encrypt VM disks
    • They provide DDoS scrubbing
    √ NSG rules can refer to workload roles rather than hard-coded IP addresses
    • They store secrets
    • They replace routing tables

    12. A web tier should accept traffic from an app tier regardless of changing VM IPs. Which design is best?
    √ Use ASGs for the tiers and reference the ASGs in NSG rules
    • Use a Key Vault access policy
    • Hard-code every current private IP
    • Disable NSGs
    • Give all VMs public IPs

    13. Which Azure Firewall rule type can allow outbound access to a specific FQDN such as www.bing.com?
    • Route-table rule
    • Network rule only
    √ Application rule
    • NSG service tag
    • DNAT rule

    14. In what order does Azure Firewall process its major rule types?
    • Network, DNAT, application
    √ DNAT, network, application
    • Application, network, DNAT
    • DNAT, application, network
    • Strictly by collection priority without type ordering

    15. Which Azure Firewall SKU adds TLS inspection and IDPS capabilities?
    √ Premium
    • Standard only
    • Developer
    • Basic
    • Consumption

    16. Which control is designed specifically for layer-7 web threats such as SQL injection and cross-site scripting?
    • ASG
    • UDR
    √ Web Application Firewall
    • NSG
    • Azure DDoS Network Protection

    17. Azure WAF managed rules are primarily based on which rule-set foundation?
    • Suricata only
    • NIST CSF
    • MITRE ATT&CK
    • CIS Controls
    √ OWASP Core Rule Set

    18. Which service is intended to absorb large layer-3/4 volumetric floods?
    • Microsoft Purview
    • Azure Bastion
    • Azure Key Vault
    • PIM
    √ Azure DDoS Network Protection

    19. Why does DDoS Protection not solve SQL injection?
    • SQL injection is a physical attack
    • SQL injection is an identity license issue
    √ DDoS Protection handles network-layer floods, while SQL injection is an application-layer attack
    • DDoS Protection cannot inspect TCP
    • DDoS Protection works only on storage

    20. What is the main security benefit of Azure Bastion?
    • It creates Kubernetes network policies
    • It provides database encryption
    √ Administrative RDP/SSH access without exposing management ports on public IPs
    • It replaces Entra ID
    • It assigns PIM roles

    21. Which Azure RBAC roles specifically allow interactive Entra sign-in to a VM?
    √ Virtual Machine Administrator Login and Virtual Machine User Login
    • Contributor and Reader
    • Global Administrator and User Administrator
    • Owner and User Access Administrator
    • Security Admin and Billing Reader

    22. A user is Contributor on a VM but cannot sign in interactively with Entra credentials. Why?
    √ Contributor manages the resource but does not grant VM logon rights
    • The VM must have no NSG
    • Contributor works only on Linux
    • The subscription is read-only
    • Contributor is a directory role

    23. Which VM extension name is associated with Entra login on Windows?
    • GuestConfiguration
    √ AADLoginForWindows
    • DependencyAgentWindows
    • AADSSHLoginForLinux
    • AzureMonitorWindowsAgent

    24. Which VM extension name is associated with Entra SSH login on Linux?
    • AzureNetworkWatcherExtension
    • AADLoginForWindows
    • CustomScriptExtension
    • OMSAgentForLinux
    √ AADSSHLoginForLinux

    25. Which two .rdp properties are required in the lab for Entra-authenticated RDP?
    • networkautodetect:i:0 and bandwidthautodetect:i:0
    √ enablecredsspsupport:i:0 and authentication level:i:2
    • gatewayusagemethod:i:2 and drivestoredirect:s:*
    • redirectclipboard:i:0 and autoreconnection enabled:i:0
    • aadauth:i:0 and promptcredentialonce:i:1

    26. Why must the Lab 4.2 route table not be associated with the jump-host subnet?
    • The subnet already has a mandatory higher-priority UDR
    √ It would send the RDP return path through the firewall and break the established inbound session
    • Jump hosts require IPv6
    • Route tables cannot be associated with jump subnets
    • Azure Firewall rejects all jump-host traffic

    27. Which mechanism provides pod-to-pod micro-segmentation inside AKS?
    • Pod Security Standards
    √ Kubernetes network policies
    • Application Security Groups on VM NICs
    • Azure Firewall DNAT
    • NSGs on the node subnet only

    28. What do Pod Security Standards primarily control?
    • Network routing between VNets
    √ Pod-level security posture such as privileged containers and risky capabilities
    • DDoS traffic
    • Key Vault key rotation
    • Azure RBAC subscription scopes

    29. Which DevSecOps technique detects vulnerable open-source dependencies?
    • SAST
    • IaC scanning
    • Secret scanning
    √ Software composition analysis (SCA)
    • DAST

    30. Which DevSecOps technique scans source code for security issues before runtime?
    • DDoS inspection
    • DAST
    • SCA
    √ SAST
    • NSG flow logs

    31. Which DevSecOps technique tests a running application from the outside?
    • Key rotation
    √ DAST
    • SCA
    • SAST
    • RBAC review

    32. Which practice detects hard-coded passwords, tokens, and API keys in repositories?
    • Bastion
    • ASG
    • DDoS Protection
    √ Secret scanning
    • SCA

    33. Which service is described as the trust anchor for keys, secrets, and certificates in Azure?
    • Azure Monitor
    √ Azure Key Vault
    • Azure Policy
    • Microsoft Sentinel
    • Azure Bastion

    34. For new Key Vault deployments, which permission model is recommended in the module?
    • Shared admin password
    √ Azure RBAC using data-plane roles
    • NSG rules only
    • Global Administrator
    • Legacy access policies only

    35. A storage account switches from platform-managed keys to a customer-managed key. What changes operationally?
    • The service stops encrypting data at rest
    • TLS is no longer needed
    • The customer must encrypt every block in application code
    • The encryption algorithm automatically changes to RSA
    √ The customer gains control over key lifecycle, rotation, audit, and revocation

    36. What is the key risk of revoking a customer-managed encryption key?
    • The VM public IP becomes static
    • DDoS Protection is disabled
    • The tenant ID changes
    • All NSGs are deleted
    √ Data protected by that key can become unreadable to the service

    37. Which capability protects data while it is actively being processed?
    √ Confidential computing using hardware-based trusted execution environments
    • Blob soft delete
    • TLS on service endpoints
    • Storage Service Encryption
    • NSG statefulness

    38. Which Microsoft service is used for data classification and sensitivity labels?
    √ Microsoft Purview
    • Microsoft Sentinel
    • Azure Bastion
    • Azure Firewall
    • PIM

    39. Which control is intended to prevent sensitive data from being shared or exfiltrated in violation of policy?
    • UDR
    • JIT access
    • VNet peering
    • ASG
    √ Data Loss Prevention (DLP)

    40. Which technique replaces sensitive values with surrogate tokens while keeping the original data protected separately?
    √ Tokenization
    • DDoS scrubbing
    • Hashing only
    • Masking
    • Peering

    41. Which cross-cloud mapping is most accurate for Azure NSGs?
    • Azure NSG ≈ AWS KMS ≈ GCP Cloud KMS
    √ Azure NSG ≈ AWS Security Group ≈ GCP VPC firewall rules
    • Azure NSG ≈ AWS Organizations ≈ GCP folders
    • Azure NSG ≈ AWS IAM Identity Center ≈ Cloud Identity
    • Azure NSG ≈ Amazon GuardDuty ≈ Security Command Center
